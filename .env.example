# Variables to set on the cabinet dashboard (never commit real values)
# Dashboard → App page → Environment variables → container: api

# JWT secret for screen tokens — generate with: openssl rand -base64 32
SCREEN_JWT_SECRET=

# Allowed CORS origins (e.g. https://yourapp.example.com)
ALLOWED_ORIGINS=http://localhost:3000
